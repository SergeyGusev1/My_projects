# Scrapy_parser_pep 🕷️

Парсер на базе фреймворка Scrapy для сбора информации о документах PEP (Python Enhancement Proposals). Собирает номера, названия и текущие статусы PEP.

## ✨ Возможности
- 🔍 Сбор данных со страниц PEP
- 📋 Извлечение номера, названия и статуса документа
- 📊 Сохранение результатов в два файла `.csv`

## 🛠 Стек технологий
- Python
- Scrapy
- CSV

## 📝 О проекте

Проект предназначен для автоматического сбора актуальной информации о всех PEP. Парсер обходит страницы с документами, извлекает ключевые данные и сохраняет их в структурированном виде для дальнейшего анализа.

## 🚀 Быстрый старт

```bash
# Клонировать репозиторий
git clone https://github.com/SergeyGusev1/scrapy_parser_pep.git
cd scrapy_parser_pep

# Создать и активировать виртуальное окружение
python -m venv venv
# Активировать (Windows)
venv\Scripts\activate
# Или (Mac/Linux)
# source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Запустить парсер
scrapy crawl pep
После выполнения парсинга результаты сохранятся в папке results/ в двух файлах .csv.
```

📁 Структура выходных файлов

```bash
Парсер создаёт два файла:

pep_list.csv — общий список всех PEP с номерами, названиями и статусами

status_summary.csv — сводка по количеству PEP в каждом статусе
```
<div align="center"> <sub>Сделано с ❤️ для анализа PEP</sub> </div> ```
